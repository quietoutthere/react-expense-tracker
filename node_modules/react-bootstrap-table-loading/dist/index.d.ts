/// <reference types="react" />
export declare type Column = {
    min: number;
    max: number;
};
export declare type TableLoadingProps = {
    columns: Column[] | number;
    lines: number;
    tbody?: boolean;
    tbodyProps?: object;
};
export declare const TableLoading: ({ columns, lines, tbody, tbodyProps }: TableLoadingProps) => JSX.Element;
