/// <reference types="react" />
import { TableBodyControlProps } from './TableBodyControl';
import { TableHeadControlProps } from './TableHeadControl';
export declare type TableControlProps = TableHeadControlProps & TableBodyControlProps & {
    total?: number;
    totalPosition?: "both" | "top" | "bottom" | "none";
    pagination?: boolean;
    page?: number;
    limit?: number;
    between?: number;
    ellipsis?: number;
    next?: boolean;
    last?: boolean;
    onClickPage?: (page: number) => any;
    isLoading?: boolean;
    loadingLines?: number;
    tableProps?: object;
};
export declare const TableControl: ({ header, sortable, onClickHeader, sort, itens, emptyMessage, clickable, onClickItem, fillEmptyColumn, total, totalPosition, pagination, page, limit, between, ellipsis, next, last, onClickPage, isLoading, loadingLines, tableProps, theadProps, trProps, tbodyProps }: TableControlProps) => JSX.Element;
