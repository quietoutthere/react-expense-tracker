/// <reference types="react" />
export declare type Sort = {
    column: string;
    direction?: "ASC" | "DESC";
};
export declare type Header = {
    name: JSX.Element | string;
    key: string;
    sort?: boolean;
    thProps?: object;
};
export declare type TableHeadControlProps = {
    header: Header[];
    sortable?: boolean;
    onClickHeader?: (sort: Sort) => any;
    sort?: Sort;
    theadProps?: object;
    trProps?: object;
};
export declare const TableHeadControl: ({ header, sortable, onClickHeader, sort, theadProps, trProps }: TableHeadControlProps) => JSX.Element;
