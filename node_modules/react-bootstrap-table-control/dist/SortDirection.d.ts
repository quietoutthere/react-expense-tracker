/// <reference types="react" />
import { Sort } from './TableHeadControl';
declare type SortDirectionProps = {
    column: string;
    sort: Sort;
};
export declare const SortDirection: ({ column, sort }: SortDirectionProps) => JSX.Element;
export {};
